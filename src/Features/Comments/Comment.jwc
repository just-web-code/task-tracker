entity Comment of AppDb {
    id        int pk autoincrement;
    taskId    int;
    authorId  int;
    body      varchar(4000);
    createdAt datetime;
    author: User { id, email, name } via authorId;
}

class CreateCommentRequest {
    body string;
}
