# Postgres (DATABASE_URL or PG_* fields):
PG_HOST=localhost
PG_PORT=5432
PG_USER=postgres
PG_PASSWORD=secret
PG_DATABASE=task_tracker

# REQUIRED — used by jwt_sign / jwt_verify.
JWT_SECRET=change-me-in-production

# PORT=8080
